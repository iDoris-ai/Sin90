//! 自己的持久化。
//!
//! Sin90 **不用内核记忆** —— 数据在清单声明的 `data_dir`（`~/.agent24/os/sin90/`）。
//!
//! 这是一个降低耦合的选择，不是将就：它让 Sin90 整条绕开 ME-3d（内核记忆回调），
//! 而那是全 ME-3 里否定用例最多、最难的一刀。你的模块如果也能自己管数据，建议
//! 照做 —— 能早很多拿到「可独立装载」。
//!
//! 注意：`data_dir` 是模块自己写的目录，而**清单不在里面**。清单决定模块的身份，
//! 放在同一棵树下就等于让模块可以在运行时改写自己的身份。
