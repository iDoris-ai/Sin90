# pilot 仓库配置 — Sin90（iDoris-ai/Sin90）
#
# 单主干仓库：PR 直接开向 main，没有 preview 集成分支（integration_branch == base_branch）。
# 评审：外部 clestons（PR-Daemon），与 Agent24 相同（用户 2026-09-23 裁决）。
#
# ⚠️ 2026-09-23 核过：main **没有任何保护**（rules/branches/main 为 []，classic protection 404）。
# 因此 git-guard 的 `merge-pr --allow-trunk` 会 fail-closed 拒绝 —— 这是对的。
# 在用户开 ruleset 之前，合并用 `gh pr merge <n> --squash`，前提仍是 clestons 已 APPROVED 且 approve 后无新 commit。
base_branch: main
integration_branch: main
protect_patterns: [release, hotfix]
remote: origin
docs_dir: docs/agent
planning_source: docs
